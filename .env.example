# Copy to .env (gitignored) and fill in.

# Your 12-word mnemonic. Account #0 (path m/44'/60'/0'/0/0) is used as the
# Monad-testnet deployer, settlement oracle, treasury, web faucet and demo
# seeder. It must hold MON for gas — top up at https://faucet.monad.xyz
MONAD_MNEMONIC=""

# Optional overrides (defaults shown)
# MONAD_RPC_URL="https://testnet-rpc.monad.xyz"
